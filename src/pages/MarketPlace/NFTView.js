import styles from "./MarketPlacePage.module.scss"
import classNames from "classnames/bind"

const cx = classNames.bind(styles)
export default function NFTview({data}){
    return (
        <div>

        </div>
    )
}